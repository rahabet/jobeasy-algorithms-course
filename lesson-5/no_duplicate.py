# Your task is to remove all duplicate words from a string, leaving only single (first) words
# entries.
# Input: 'alpha beta beta gamma   gamma gamma delta alpha beta beta gamma gamma gamma delta'
# Output: 'alpha beta gamma delta'

string_1 = input('Enter a string: ')

def no_duplicate(string):
    pass

print(no_duplicate(string_1))
